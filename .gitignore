# Claude Code / agent tooling -- not part of the public project
.claude/
subagents/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Generated at runtime (rebuilt via scripts/ingest.py)
*.db

# OS
.DS_Store

# Kept out of the public repo by request
Summer School Foundry Local Plan.pdf
videoframe_579.png

# Claude Design handoff bundle (source for static/) -- its own uploads/ context dump
# includes a copy of subagents/ and other project files pulled in for design reference;
# keep the whole bundle out of git rather than the pushed public repo.
shsu-transfer-sohbet-aray-z/
*.zip
