"""RAG pipeline package: loader -> chunker -> embedder -> store -> retriever -> generator.

Plain Python, no Streamlit dependency -- importable and testable on its own
(locked architectural rule, see PROJECT_PLAN.md).
"""
